<!DOCTYPE html>
<html lang="en">

<head> <% include ./partials/head.ejs %> </head>

<body>
    <header> <% include ./partials/header.ejs %> </header>

    <main>
        <div class="box-left">

            <!-- IF USER IS NOT LOGGED IN -->

            <% if (!currentUser) { %>
                <h1 class="up-z">Save your favorite code snippets as recipe cards.</h1>

                <div id="registration" class="row-flex">
                    <a href="/sessions/new"><h2 class="bold-link tx-green">Log in</h2></a>
                    <a href="/users/new"><h2 class="bold-link">Sign up</h2></a>
                </div>

            <!-- IF USER IS LOGGED IN -->

            <% } else { %>
                <h1 class="up-z tx-green">Let's get cooking, shall we?</h1 class="up-z">

                    <div class="row-flex distancer">
                        <a href="/app"><h2 class="choices">Go to recipes.</h2></a>
                        <a href="/app/add-recipe"><h2 class="choices">Add a new recipe.</h2></a>
                    </div>

            <% } %>
        </div>

        <div class="box-right">
            <div class="img-dim"></div>
            <img src="../images/ramen-square.jpg" class="img-circle" />

            <% if (currentUser) { %>
                <h4>Welcome, Chef @<%= currentUser.username %>!</h4>

                <% include ./partials/logout.ejs %>
            <% } %>
        </div>
    </main>

    <footer> <% include ./partials/footer.ejs %> </footer>
</body>
<html>