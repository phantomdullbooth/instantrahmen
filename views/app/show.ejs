<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head.ejs %>
</head>

<body>
    <!-- HEADER [ TITLE, QUOTE]  -->
    <header>
        <% include ../partials/header.ejs %>
    </header>

    <main>
        <div class="box-left">
            <h3>Ahh, the <span class="tx-green"><%=recipe.title%></span>.<br />Excellent choice.</h3>

            <div class="row-flex distance">
                <a href="/<%=recipe.id%>/edit-recipe"><h6 class="nextup">Edit this recipe.</h6></a>
                <form action="/<%=recipe.id%>?_method=DELETE" method="POST">
                    <button type="submit" name="delete-recipe" class="btn-less">
                       <h6 class="hint ">Delete this recipe.</h6>
                    </button>
                </form>
            </div>

            <h5>About this recipe: <%=recipe.description%> </h5>
            <h5 class="unstack"><%=recipe.recipe%></h5>
        </div>

        <div class="box-right">
            <div class="img-dim"></div>
            <img src="<%=recipe.image%>" class="img-circle" />


            <form action="/sessions/app?_method=DELETE" method="POST">
                <button type="button" name="log-out" class="btn-less"><h6 class="hint">Log out</h6></button>
            </form>
        </div>
    </main>


    <footer><% include ../partials/footer.ejs %></footer>
</body>

</html>